### 安装命令

```bash
npm install -g @fission-ai/openspec@latest
```

再次调用上命令，可以升级，目前已经升级到1.2.0
### 使用

```bash
cd your-project
openspec init
```

进入claude code后
1. opsx:new 某个功能

### 查看进度

```bash
openspec view
```