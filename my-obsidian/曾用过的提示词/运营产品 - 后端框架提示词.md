你是一名 **资深 Python 后端工程师 & 工程规范制定者**。  
请帮我 **从 0 创建一个 Python 后端工程骨架**，用于后续开发一个 FastAPI 服务。

⚠️ 本阶段 **只做工程初始化，不实现任何具体业务逻辑**。

---

## 一、工程目标（非常重要）

- 这是一个 **长期演进的后端工程**
- 当前目标：
    - 工程结构清晰
    - 依赖管理现代化
    - 能直接启动一个 FastAPI 示例应用（health check 即可）
- **业务逻辑将在下一阶段单独实现**

---

## 二、技术与工具约束（必须遵守）

### Python & 工程管理

- Python **3.10+**
    
- 使用 **uv** 作为：
    
    - Python 版本约束
        
    - 虚拟环境
        
    - 依赖管理工具
        
- 使用 `pyproject.toml`
    
- **不得出现 requirements.txt**
    

---

### Web 框架

- FastAPI（仅初始化，不写真实接口）
    
- Uvicorn 作为 ASGI Server
    

---

## 三、工程结构（请严格按此生成）



---

## 四、FastAPI 要求（最小即可）

- `main.py` 中：
    
    - 创建 FastAPI app
        
    - 引入一个 `/health` 接口
        
    - 返回 `{ "status": "ok" }`
        
- 不要实现任何业务相关 API
    
- 不要引入数据库、LLM、鉴权等内容
    

---

## 五、pyproject.toml 要求（重点）

- 使用 uv 兼容格式
    
- 明确声明：
    
    - Python 版本
        
    - FastAPI
        
    - Uvicorn
        
- 保持 **最少依赖**
    
- 注释说明各 section 的作用
    

---

## 六、README.md 内容要求

请写清楚：

1. 这个工程是做什么的（一句话即可）
    
2. 如何使用 uv 创建环境
    
3. 如何安装依赖
    
4. 如何启动 FastAPI（开发模式）
    

---

## 七、工程原则（非常重要）

- **只做骨架，不做业务**
    
- 所有文件内容都要：
    
    - 简洁
        
    - 可读
        
    - 带必要注释，说明“以后这里干什么”
        
- 这是一个 **为下一阶段业务开发服务的地基工程**
    

---

## 八、最终交付目标

> 当我执行：
> 
> `uv run uvicorn app.main:app --reload`
> 
> 能成功启动服务，并访问 `/health`